"""Phase 2A Pass 4 — Relationship Building.

Per-chunk LLM extraction of `(subject, predicate, object)` triples between
entities Pass 2 already wrote from the same chunk. Spec: PROJECT_PLAN.md
Phase 2 § Pass 4.
"""
